---
url: /01.指南/01.简介/01.简介.md
---

# 简介

## ✨ 项目简介

**OsEasy-ToolKit** 是一个基于 Python Tkinter 重构的噢易多媒体教学系统学生端工具箱，提供解锁、进程管理、广播控制等核心功能。

> 之所以使用 Python Tkinter 重构，是因为学校机房的网速慢到极点，原版即使 38MB 的工具箱也要下载很久。于是基于 [ZiHaoSaMa66](https://github.com/ZiHaoSaMa66) 的 [OsEasy-ToolBox](https://github.com/ZiHaoSaMa66/OsEasy-ToolBox) 重构。

## 🚀 快速使用

```bash
# 安装依赖
pip install -r requirements.txt

# 运行
python main.py

# 打包发布
python build.py          # 正式版
python build.py --debug  # 调试版
```

## 📦 核心功能

| 模块 | 功能 |
|------|------|
| **进程管理** | 挂起/恢复学生端进程、切换 MMPC 根服务、粘滞键后门注册 |
| **解锁管理** | 删除键盘锁/控屏/黑屏安静、解锁网络/USB 限制、文件备份恢复 |
| **广播管理** | 替换 ScreenRender 拦截命令、窗口化/全屏广播、快捷键支持 |
| **广播命令** | 手动输入/编辑命令、从教师机 IP 生成、从日志提取、导入/导出 |
| **DLL 工具** | USB 管控启停、网络管控开关 |

## ⚠️ 注意事项

* 需要以 **管理员权限** 运行
* 适用于 **噢易 V10.8+** 学生端
* 使用前请确保已 **备份重要文件**
* 若在学校因使用工具箱被物理制裁，一切责任与作者无关

## 🌟 技术特点

* **解耦架构**：配置、服务、页面完全分离，代码结构清晰，易于维护和扩展
* **极致轻量**：基于 tkinter + ttk GUI 框架，打包体积极小，网速慢也能快速下载使用
* **功能全面**：支持进程管理、解锁管理、广播管理、广播命令、DLL 工具等核心功能
* **专注场景**：专为噢易 V10.8+ 学生端设计，覆盖机房日常使用的各类管理需求

## 📄 许可

MIT License · [GitHub 仓库](https://github.com/NYSkyfox/OsEasy-ToolKit)

---

---
url: /01.指南/01.简介/02.网络解锁.md
description: 手动解锁噢易学生端网络限制
---

# 🌐 如何手动解锁网络限制

> 即使不下载 OsEasy-ToolKit 工具，你也可以手动输入几条命令来解锁噢易学生端的网络限制。本教程手把手教你操作。

## 适用场景

* 在信息课上，学生端电脑被牢逝限制了网络
* 想了解解锁网络的底层原理
* 仅需恢复上网功能，不需要解锁其他限制

::: info 一：打开 CMD（命令提示符）

**操作步骤：**

1. 点击任务栏搜索按钮，打开搜索菜单
   ![搜索 cmd](/images/unlock_network/1.png)
2. 输入 `cmd`，在搜索结果中找到"命令提示符"，右键点击"命令提示符" → 选择"以管理员身份运行"，点击"是"确认
   ![以管理员身份运行](/images/unlock_network/2.png)
3. 出现 CMD 黑色窗口即成功
   ![CMD 成功打开](/images/unlock_network/3.png)

> Tips：机房电脑通常默认登录管理员账户，你也可以直接 Win+R 运行 cmd ，无需额外操作。
> :::

::: info 二：停止 MMPC（学生端根服务）

```batch
sc stop mmpc
```

停止学生端根服务（MMPC），这是管控学生端的中心服务（该服务没什么实质性的管控，但会实时监控学生端是否被偷偷关闭，若检测到学生端被关闭，就会自动重启相关进程。所以这是解锁的第一步）

**操作示例：**

1. 在 CMD 中输入命令 `sc stop mmpc` 并回车
   ![输入 sc stop mmpc](/images/unlock_network/4.png)
2. 显示 `SEVER_NAME xxxxxxx` 即表示执行成功。命令不区分大小写
   ![执行成功](/images/unlock_network/5.png)
   :::

::: info 三：停止 OeNetlimit（网络限制服务）

```batch
sc stop oenetlimit
```

停止网络过滤和限制服务，解除驱动级服务对网络的限制。

**操作示例：**

1. 在 CMD 窗口中输入 `sc stop oenetlimit` 并回车

   ![输入 sc stop oenetlimit](/images/unlock_network/6.png)
2. 显示 `STATE: 1 STOPPED`（或 `STOP_PENDING`）即表示执行成功

:::

::: info 四：结束 DeviceControl\_x64.exe（设备控制进程）

结束设备控制进程，解除网络和程序限制

**操作示例：**

1. 右键任务栏空白处 → 选择"任务管理器"
   ![右键任务栏打开任务管理器](/images/unlock_network/7.png)
2. 会弹出一个新的白色窗口，点击窗口左下角的"详细信息"展开完整列表
   ![点击详细信息](/images/unlock_network/8.png)
3. 在进程列表中找到 `DeviceControl_x64.exe` → 右键 → 选择"结束任务"
   ![结束进程](/images/unlock_network/9.png)
   :::

总共就 \*\*4 个步骤 \*\*，逐步进行即可，熟练的话十几秒就能搞定。

## 常见问题

::: info 关闭服务时报错

### Q：执行 `sc stop ` 提示服务名无效？

```
[SC] OpenService FAILED 1060: 指定的服务未安装。
```

这说明你输错了，重新输。

### Q：执行 `sc stop ` 提示服务未启动？

```
[SC] OpenService FAILED 1060: 指定的服务未启动。
```

这说明你输重复了，已经执行成功过了，请去执行下一步吧。
:::

### Q：重启电脑后网络又被限制了？

这是正常的——学校机房将计算机设为了每次重启时还原，也就是说每次重启都不会保留你的任何内容，我也没办法，只能每次上课时进行解锁

### Q：解锁后任务栏的网络图标仍然显示"无Internet"或黄色感叹号？

这是正常现象。学生端网络限制解除后，系统网络状态指示可能需要一段时间才能刷新。**请直接打开浏览器访问任意网站**，你会发现网络实际上已经恢复正常了，不需要理会网络图标的显示状态。

---

---
url: /01.指南/01.简介/03.下载.md
---

# 下载 OsEasy-ToolKit

## 最新版本

## 本地下载

---

---
url: /@fragment/coverImg.md
---

# ArticleBanner 测试

我是一个测试 `ArticleBanner` 功能的文档，仅在没有侧边栏的文章中出现。

---

---
url: /@pages/loginPage.md
---


---

---
url: /@pages/riskLinkPage.md
---


---

---
url: /examples/articlePage/aside.md
---


---

---
url: /examples/articlePage/doc.md
---


---

---
url: /@pages/categoriesPage.md
---


---

---
url: /@pages/archivesPage.md
---


---

---
url: /01.指南/目录.md
---


---

---
url: /@pages/articleOverviewPage.md
---


---

---
url: /@pages/tagsPage.md
---

