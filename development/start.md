# 🚀 快速使用

```bash
# 安装依赖
pip install -r requirements.txt

# 运行
python main.py

# 打包发布
python build.py          # 正式版
python build.py --debug  # 调试版
```

# 🌟 技术特点

- **解耦架构**：配置、服务、页面完全分离，代码结构清晰，易于维护和扩展
- **极致轻量**：基于 tkinter + ttk GUI 框架，打包体积小
- **功能全面**：支持进程管理、解锁管理、广播管理、广播命令、DLL 工具等核心功能
- **专注场景**：专为噢易 V10.8+ 学生端设计，覆盖机房日常使用的各类管理需求
